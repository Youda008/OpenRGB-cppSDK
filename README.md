# OpenRGB-cppSDK
C++ client/SDK for the OpenRGB service

... work in progress ...

## Build (Msys2)
```
mkdir build-release
cd build-release
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release ../
make
```
